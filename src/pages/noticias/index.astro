---
import Layout from '../../layouts/Layout.astro';
import NewsCard from '../../components/NewsCard.astro';
import { getCollection } from 'astro:content';

const noticias = await getCollection('noticias');
noticias.sort((a, b) => b.data.fecha.getTime() - a.data.fecha.getTime());
---

<Layout
  titulo="Noticias del Torrenueva Futsal"
  descripcion="Noticias, resultados, fichajes y novedades del Club Torrenueva Futsal. Sigue toda la actualidad del equipo de fútbol sala de Torrenueva."
>
  <section class="seccion">
    <h1 class="titulo-seccion">Noticias</h1>
    <ul class="cuadricula-2" role="list">
      {noticias.map(n => (
        <li>
          <NewsCard
            titulo={n.data.titulo}
            fecha={n.data.fecha}
            imagen={n.data.imagen}
            descripcion={n.data.descripcion}
            slug={n.id.replace(/\.md$/, '')}
          />
        </li>
      ))}
    </ul>
  </section>
</Layout>
